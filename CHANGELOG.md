# Changelog

## 未发布

- **结果查看器**（插件更新即生效，App 不用重装）：
  - 工具行展开后有"完整内容"：看完整的 diff（逐行对比，大段未改动的折叠）、完整命令输出。
  - 工具涉及的文件显示为按钮，点开直接看。
  - 对话菜单里的"工作区文件"可以浏览整个工作区：文本、Markdown（预览/源码切换）、图片、视频、音频；GBK、UTF-16 编码的日志也能正确显示。
  - 只能看该对话所在工作区里的文件（包括拦截指向外面的快捷方式）。密钥、证书、`.env`、名字带"密码/password/token"的文件不显示；内容里有私钥的文件拒绝打开。

## 1.2.1 — 2026-09-21

- **安全加固**：公开版关闭 WebView 远程调试。
  - 1.2.0 公开版开着调试，拿到手机 USB 调试授权的人可以用 DevTools 读取网关 Cookie、操作 DSH。
  - 私有构建（`bash build.sh`）仍保留调试，方便开发时用 adb 做测试。
  - 建议 1.2.0 用户升级（同一签名，可直接覆盖安装）。

## 1.2.0 — 2026-09-21 · 首个公开版本

- **首次配置页**：公开版不预置服务器地址，首次打开时填写。
  - 地址校验严格：只接受 http/https，拒绝带用户名、空格或主机名不合法的地址。
  - 填错时原样退回并提示；在公网上用 http 会给出警告。
- **切换服务器**：点界面顶部的连接状态，或在"连不上"页面点"更换服务器地址"；切换后后台提醒自动重连到新地址。
- **`trustedHosts` 配置**：支持异地组网直连等非回环地址的部署。
  - 匹配规则与 DSH 自身的信任栅栏一致：`:80` / `:443` 视为显式端口；格式不规范的条目会让插件在加载时直接报错。
- **`tools/forward.mjs`**：把 DSH 转发到一个私有地址（异地组网 / 局域网），拒绝通配和公网地址。
- **构建脚本可移植**：
  - SDK / JDK 路径从环境变量或 `local.properties` 读取；
  - `build.sh public` 构建不含私有地址的发布包。
- 项目改名为 **dsh-pager**，以 MIT 许可开源。

## 1.1.x — 2026-09-21（未公开）

- **后台提醒**：前台服务 + 专用低频通知流。
  - 覆盖完成、出错、提问、审批；审批可在通知栏直接允许或拒绝。
  - 断线补发；开机和升级后自动恢复；网络切换时立即重连。
- 权限申请改到回到前台时发起、每次启动最多一次（修复锁屏状态下申请被系统静默取消的问题）。

## 1.0 — 2026-09-21（未公开）

- DSH 插件在 `/m` 提供手机界面和折叠后的接口：打开 4 MB 级会话只需下载 20–30 KB。
- 安卓 WebView 外壳，无 Gradle 构建。
